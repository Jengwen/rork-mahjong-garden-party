workflows:
  ios-workflow:
    name: Mahjong Garden Party - iOS
    instance_type: mac_mini_m2
    max_build_duration: 60
    integrations:
      app_store_connect: "Codemagic CI"  # name of your App Store Connect API key integration in Codemagic settings
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.mahjonggardenparty
      vars:
        XCODE_PROJECT: "ios/MahjongGardenParty.xcodeproj"
        XCODE_SCHEME: "MahjongGardenParty"
        APP_STORE_APPLE_ID: 6793677304
      xcode: latest
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
          include: true
          source: true
    scripts:
      - name: Resolve Swift Package dependencies
        script: |
          xcodebuild -resolvePackageDependencies \
            -project "$XCODE_PROJECT" \
            -scheme "$XCODE_SCHEME"

      - name: Increment build number
        script: |
          cd $(dirname "$XCODE_PROJECT")
          agvtool new-version -all $(($(app-store-connect get-latest-app-store-build-number "$APP_STORE_APPLE_ID") + 1))

      - name: Build ipa for distribution
        script: |
          xcode-project build-ipa \
            --project "$XCODE_PROJECT" \
            --scheme "$XCODE_SCHEME"

    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - $HOME/Library/Developer/Xcode/DerivedData/**/Build/**/*.dSYM

    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
        submit_to_app_store: false  # flip to true once you're ready for App Store review, not just TestFlight
