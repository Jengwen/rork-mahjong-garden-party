workflows:
  ios-workflow:
    name: Mahjong Garden Party - iOS
    instance_type: mac_mini_m2
    max_build_duration: 60
    integrations:
      app_store_connect: "Codemagic CI"   # used for uploading to TestFlight
    environment:
      ios_signing:
        # Reference names as shown in Codemagic > Settings > Code signing identities
        provisioning_profiles:
          - Mahjong Garden
        certificates:
          - Codemagic Distribution
      vars:
        XCODE_PROJECT: "ios/MahjongGardenParty.xcodeproj"
        XCODE_SCHEME: "MahjongGardenParty"
        APP_STORE_APPLE_ID: 6793677304
      xcode: latest
    triggering:
      events:
        - push
      branch_patterns:
        - pattern: main
          include: true
          source: true
    scripts:
      - name: Apply uploaded signing files to the Xcode project
        script: |
          xcode-project use-profiles

      - name: Resolve Swift Package dependencies
        script: |
          xcodebuild -resolvePackageDependencies \
            -project "$XCODE_PROJECT" \
            -scheme "$XCODE_SCHEME"

      - name: Build ipa for distribution
        script: |
          # Ask App Store Connect for the highest build number already uploaded, then add 1.
          BUILD_NUM=$(($(app-store-connect get-latest-app-store-build-number "$APP_STORE_APPLE_ID") + 1))
          echo "Building with CFBundleVersion $BUILD_NUM"
          xcode-project build-ipa \
            --project "$XCODE_PROJECT" \
            --scheme "$XCODE_SCHEME" \
            --archive-xcargs "COMPILER_INDEX_STORE_ENABLE=NO CODE_SIGN_STYLE=Manual CURRENT_PROJECT_VERSION=$BUILD_NUM"

    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log

    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
        submit_to_app_store: false
